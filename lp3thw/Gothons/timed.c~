#include <unistd.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <Python.H>
#define MAX_SECONDS 10

int main(int argc, char *argv[])
{
    /* if (fork() == 0) { */
    /* 	unsigned secsleft; */
    /* 	alarm(MAX_SECONDS); */
	
    /* } */
    printf("working girl!\n");
    return 0;
}
